# Default image for Linux runners
-P ubuntu-latest=ghcr.io/catthehacker/ubuntu:act-latest

# Windows runner image
-P windows-latest=ghcr.io/catthehacker/win-server:latest

# macOS runner image (runs on Linux but with macOS environment variables)
-P macos-latest=ghcr.io/catthehacker/ubuntu:act-latest

# Additional configuration
--use-gitignore
--bind
--rm
--reuse
--secret-file .env.act
